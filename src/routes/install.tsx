import{createFileRoute}from"@tanstack/react-router";export const Route=createFileRoute("/install")({head:()=>({meta:[{title:"Install — UI Library"},{name:"description",content:"Install UI Library components and scoped settings in five exact steps."},{property:"og:title",content:"Install — UI Library"},{property:"og:description",content:"Install UI Library components and scoped settings in five exact steps."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:Install});const steps=[['Install the package','pnpm add @ui-library/ui'],['Import tokens once at the app entry',"import '@ui-library/ui/tokens.css';"],['Import the components you use',"import { Avatar, Badge, CurrencyField, Table, Timeline } from '@ui-library/ui';"],['Import Settings',"import { ThemeScope, type ThemeSettings } from '@ui-library/ui/settings';"],['Keep settings in your project',`const projectSettings: ThemeSettings = { /* your values */ };\n\n<ThemeScope settings={projectSettings}>\n  <App />\n</ThemeScope>`]];function Install(){return <main className="page"><div className="page-kicker">Setup / 05 steps</div><div className="page-heading"><div><h1>Install</h1><p>From shelf to project, without bringing the storefront along.</p></div></div><div className="install-list">{steps.map(([title,code],i)=><section key={title}><span>{String(i+1).padStart(2,'0')}</span><div><h2>{title}</h2><pre><code>{code}</code></pre></div></section>)}</div><p className="install-note">UI Library itself is not installed. The website is the showcase. The package is what gets installed.</p></main>}
