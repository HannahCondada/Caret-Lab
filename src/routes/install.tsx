import{createFileRoute}from"@tanstack/react-router";export const Route=createFileRoute("/install")({head:()=>({meta:[{title:"Caret Library"},{name:"description",content:"Install Caret Library components and scoped settings in five exact steps."},{property:"og:title",content:"Install — Caret Library"},{property:"og:description",content:"Install Caret Library components and scoped settings in five exact steps."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:Install});const steps=[['Install the package','pnpm add @caret-lab/ui'],['Import tokens once at the app entry',"import '@caret-lab/ui/tokens.css';"],['Import the components you use',"import { Avatar, Badge, CurrencyField, Table, Timeline } from '@caret-lab/ui';"],['Import Settings',"import { ThemeScope, type ThemeSettings } from '@caret-lab/ui/settings';"],['Keep settings in your project',`const projectSettings: ThemeSettings = { /* your values */ };\n\n<ThemeScope settings={projectSettings}>\n  <App />\n</ThemeScope>`]];function Install(){return <main className="page"><div className="page-kicker">Setup / 05 steps</div><div className="page-heading"><div><h1>Install</h1><p>From shelf to project, without bringing the storefront along.</p></div></div><div className="install-list">{steps.map(([title,code],i)=><section key={title}><span>{String(i+1).padStart(2,'0')}</span><div><h2>{title}</h2><pre><code>{code}</code></pre></div></section>)}</div><p className="install-note">Caret Library itself is not installed. The website is the showcase. The package is what gets installed.</p></main>}
